export interface ServiceProps {
  id: string;
  title: string;
  description: string;
  imageSrc: string;
  imageAlt: string;
}

export interface NavLinkProps {
  id: string;
  label: string;
  href: string;
}